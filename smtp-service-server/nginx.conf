upstream smtp_service_server_cluster {
    server 127.0.0.1:5001 max_fails=1 fail_timeout=3s;
}

server {
    server_name subdomain.maindomain;

    location / {
        proxy_pass http://smtp_service_server_cluster;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# make sure to add SSL cerificate by cerbot that will modify this file automaticly just follow its docs